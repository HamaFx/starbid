import { NextResponse } from "next/server";

export function GET() {
  const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>StarBid galaxy</title><style>body{margin:0;background:#05050a;color:#fff4e0;font:14px system-ui,sans-serif}main{padding:16px}h1{font-size:16px;margin:0 0 12px}.star{display:flex;justify-content:space-between;border-bottom:1px solid #ffffff1a;padding:10px 0}.demo{color:#ffb627;font-size:10px;text-transform:uppercase}</style></head><body><main><h1>StarBid / live galaxy</h1><div id="stars" aria-live="polite">Loading…</div></main><script>const root=document.querySelector('#stars');fetch('/api/public/stars').then(r=>{if(!r.ok)throw new Error();return r.json()}).then(({data=[]})=>{root.replaceChildren(...(data.length?data.map((s,i)=>{const row=document.createElement('div');row.className='star';const label=document.createElement('span');label.textContent='#'+(i+1)+' '+s.name+(s.isDemo?' demo':'');const total=document.createElement('strong');total.textContent='$'+(s.totalBidCents/100).toFixed(2);row.append(label,total);return row}):[Object.assign(document.createElement('span'),{textContent:'No stars yet.'})]))}).catch(()=>{root.textContent='Unable to load galaxy'})</script></body></html>`;
  return new NextResponse(html, { headers: { "Content-Type": "text/html; charset=utf-8", "Content-Security-Policy": "default-src 'none'; script-src 'unsafe-inline'; connect-src 'self'; style-src 'unsafe-inline'; frame-ancestors *", "Cache-Control": "public, max-age=30" } });
}
